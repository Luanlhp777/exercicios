const prompt = require("prompt-sync")();

// Crie um programa que mostre todos os números pares de 0 a 50.

for (var num = 1; num <= 50; num++) {

    if (num % 2 == 0) {

        console.log(num);

    }
}