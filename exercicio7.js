const prompt = require("prompt-sync")();

// Crie um programa que mostre na tela os números de 1 até 20 utilizando o laço for.

for (var cont = 1; cont <= 20; cont++) {

    console.log(`${cont}`);

}